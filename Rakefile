# require 'pg'


# task :setup do
#   p 'Creating databases...'

#   %w[thermostat thermostat_test].each do |database|
#     connection = PG.connect
#     connection.exec("DROP DATABASE #{database};")
#     connection.exec("CREATE DATABASE #{database};")
#     connection = PG.connect(dbname: database)
#     connection.exec('CREATE TABLE temperature(id SERIAL PRIMARY KEY, first_name VARCHAR(60));')
#   end
# end